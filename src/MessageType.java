/**
 * Each type of message that could be needed for nodes to send eachother in the SynchGHS algorithm
 */

public enum MessageType {
    MWOE_BROADCAST, CONVERGECAST, LEADER_BROADCAST, MERGE, SHUTDOWN_BROADCAST
}
